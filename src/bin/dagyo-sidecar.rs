fn main() {
    println!("Hey there, I'm a dagyo sidecar! I get deployed alongside every executor.");
    println!("Later I will talk to the message broker so executors don't have to.");
    println!("I'm not implemented yet!");
    std::thread::sleep(std::time::Duration::MAX);
}
